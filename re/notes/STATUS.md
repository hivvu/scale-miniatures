# RE status (2026-09-14)

| module | status |
|---|---|
| PKLITE unpack | done, tools/mm/pklite.py == UNP 4.11 output |
| Code card bypass | done (re/unpacked/MICRONCC.EXE, notes/00-overview.md) |
| Timer / frame sync / IRQs | read (notes/30-timer.md); tick = VGA frame 70.086 Hz |
| Keyboard | read (notes/20-input.md); key bitmask ds:[107c] |
| Joystick and mouse | done (notes/20-input.md): fn 2d5b's own read of the game port (2f9e/2fcc/2ffe) and int 33h, then fn 2e6c/2eb3 (X axis steers, buttons 1 and 2 brake and accelerate, the Y axis compares are dead code) and fn 2e02 (pointer distance from the middle, recentred every frame). src/hal/input/Analogue.ts maps them onto the Gamepad API and a locked pointer; test/engine/devices.test.ts |
| File loaders | mapped (notes/40-file-loaders.md) |
| LZ codec | done, tools/mm/lz.py; 0x60-0x6F reverse-copy fixed 2026-09-11; byte-exact vs game memory (VH0) |
| MAP / CT / PR banks / PAL | decoded, full-track render verified (build/assets-debug/tracks) |
| COL / DIR / LEV / BRK semantics | done (notes/80-ai-collision.md): COL 1 bpp per 8x8 cell, DIR byte per tile (AI dir + terrain type), LEV byte per block, BRK per progress cell |
| VH0 sprite layout, rotation table, sprite renderer (7ce0/7d73/8bab/8c6a) | done: table byte-exact vs DOSBox-X dump (notes/61-sprites.md, tools/mm/sprites.py) |
| COMPRESS.PI* usage (front-end graphics) | done: 7 LZ banks from segment 1B78, image headers ds:0A14, object records ds:0B7C (notes/50-frontend.md) |
| Race main loop fn 3039, input byte layout (notes/20-input.md) | read |
| Physics / AI / collision / camera / ranking | transliterated to Python (tools/mm/sim/race.py) and TS (src/engine/race.ts); step-exact vs 17 DOSBox-X traces: round 2 track 1 (steer/ram/wall/collide/brake/current/whirlpool states 1/7/2, finish = 748 steps incl. the player finishing, the 100-step grace period and the loop exit), rounds 1/3/4/5/6/7/8/9 track 1 (300 steps each); collision pair order + bx quirk of fn 5921/5960 fixed 2026-09-11; terrain handlers 657e/6622/6674/669b/66ed/683b/683c/6ac2/6882/69c5 ported; fn 79fd (round 7 shells) and round 9 banner states 0xF/0x10 ported (not exercised by the captures yet); round 3 pockets (6ae5, states 4/5/0xE) ported and verified with trace_r3pocket (2026-09-12); head to head came in later, see its own row |
| Cheats / pause / GAME OPTIONS | done (notes/53-cheats.md): fn 2770's key loop (F1..F4, Return, Esc and the eight-scancode sequence **2 5 0 1 1 9 6 8** at [0f6b]), fn 35f0 (Space pauses, applies the CHEATS.BIN spot under the car and offers F12 / F1+F2 / F2+F3), fn 1398 (keypad +/- pick the race from the card) and fn 11f8's fn 179b wait. The cheat gives ten lives after every race. Also done: fn 2be8 (PLAY WHICH GAME SET ?), fn 92f0 (F5 REDEFINE KEYS), fn 2ab5/2b93 (F7, the joystick calibration: F7 is that screen, not a sound test), fn 2a13 (SETTINGS.DAT written back, in localStorage) and fn 3ad0 (AUTO, timed in the page) |
| Sound | ported (notes/90-sound.md): DRIVERn.BIN = Sound Images Generation 2, loaded at segment 1424, 17 functions dispatched on ah. DRIVER1 (OPL2) carries the game's whole sound set: a bank of 18 short sequences at 0x65b4 and 128 instrument records at 0x67d1; sounds >= 0x40 are the four engine records at driver:0008. Sequence format = MIDI-style delta/event, decoded by tools/mm/sound.py. **8 songs of 4-8 tracks each** in the song bank at 0x13f6. src/engine/sound/driver.ts (the sequencer), src/engine/sound/opl2.ts (the YM3812) and src/hal/audio/SoundOutput.ts (Web Audio) play them in the browser; game.html has the music of every front-end screen, the race's 24 sound-effect calls and the engine note of each car (fn 7b46 + the fn 7cae random). fn 7af8 (silencing the engines by zeroing the speeds) came in with the head to head. **Validated against the real driver**: tools/gt_opl.py dumps the driver's work area (register shadow 114d, channel records 1271, queues 13d1/13d9, tempo accumulators 1261/1269) every few game ticks under DOSBox-X with `sbtype=sb2` and SETTINGS.DAT word 3 = 1, and test/engine/opl_trace.test.ts seeds SoundDriver with one sample, runs the ticks in between and matches the next byte for byte. Not ported: DRIVER2 (PC speaker); the engine notes (fn 7b46) are still disassembly-only, since the game rewrites the pitch between ticks |
| Intro (SM.EXE, GFX1.GFX, ANTIFONT.BIN) | done (notes/52-intro.md): MICRO.COM chains SM.EXE then MICRO.EXE. SM.EXE is a plain MZ, no relocations, cs = ds = the load segment; it reads gfx1.gfx and antifont.bin whole into two 64000-byte blocks and draws mode 13h straight to A000 at the retrace. src/engine/intro.ts is the whole of it (letters table at [001e], the two sliding words, the diagonal shine, the proportional antifont, the A+B build stamp); 313 frames, ended early by a click (int 33). tools/gt_intro.py breaks at cs:097f once per frame; test/engine/intro.test.ts is byte-exact over the first 170 |
| DOSBox-X ground truth | headless debugger driver (tools/dbg_drive.py, gt_frame.py, gt_race.py); VRAM + memory dumps in build/golden/gt/ |
| TypeScript engine | src/engine/{memory,race}.ts = port of tools/mm/sim/race.py; test/engine/trace.test.ts replays every build/golden/gt/trace_* byte-exact (steer, ram, wall, collide) |
| TypeScript | package.json/vite/vitest; src/data decoders (LZ, palette, tilemap, blocks, sprites) pass all 42 golden hashes (41 LZ plus the PKLITE image); viewer.html works with the real files (Phase 1 deliverable) |
| Widescreen (port only) | done: src/engine/viewport.ts derives every geometry constant from one width and height, `DOS_VIEWPORT` = the original 256x200 exactly and the default everywhere. render.ts and race.ts carry no geometry literals any more. Opt-in in game.html (a picker, remembered in localStorage) and race.html (?width=&height=). The Head to Head scoring area grows with the view, because fn 4fd1's thresholds are literally width-24 / height-24. test/engine/viewport.test.ts pins the DOS values; test/engine/widescreen.test.ts renders the same frozen state narrow and wide and requires the shared columns to be identical (81 cases over 16 traces) |
| TypeScript renderer | src/engine/render.ts: tiles + animated tiles (round 2 plughole map words, rounds 1/3/5 water tile) + fine scroll + priority overlays + sprites/shadows/foam/skids + particles + state animations (sink 880a, respawn 82be) + finishing-position markers (9076) + HUD (8dfc); helicopter rotors (843d), round 8 tile blocks (8a2b), round 9 time display + 40 px sprites; pixel-exact vs about 1300 VRAM captures over all 9 rounds incl. whirlpool and finish traces (test/engine/frame.test.ts) |
| Race setup from files | src/engine/setup.ts = fn 37fc/3b50/3c09/458b/482f/4611/45e5/2d00/2770 + fn 3039 prologue; DS built from MICRO.EXE (src/data/pklite.ts depacker) + game files is byte-exact vs the DOSBox-X start-of-race dump outside ISR/menu regions (test/engine/setup.test.ts); race.html no longer needs the golden dump |
| Front end (graphics engine) | done: src/engine/frontend.ts = fn 0630/053a/04b8/05b4/05f3/06cc/0710/07e5/0823/0862/0876/089c/08bc/08f0/0910/0929/0999 over the 256x200 buffer at segment 6D78; pixel-exact vs DOSBox-X captures |
| Front-end screens | src/engine/screens.ts: options (2770), title (0100), SELECT GAME (0220), one-player menu (02e0), character select (09e0 + 0d1f/0db0/0f17/0e02/0f3c), pre-race card (11f8 + 19f2/1867/01de), results board (13e4), verdict (1c1b). All eleven verified pixel-exact against DOSBox-X captures in test/engine/frontend.test.ts (build/golden/gt/front, results, board) |
| Challenge sequence | src/engine/sequence.ts = fn 10a0 + fn 115c head/tail + fn 11d5 + fn 1a82; game.html plays the card, the race and the verdict from one shared data segment (test/engine/sequence.test.ts) |
| Ground truth for the front end (no sound: the DOS captures run with SETTINGS.DAT word 3 = 0) | tools/gt_front.py (menus), gt_results.py (end of race + verdict), gt_board.py (pre-race card + RESULTS!! board; ends each race by poking [26c6]=2/[26cc]=1 and fills the opponents' characters so fn 1a4a does not wait for input) |
| Front-end input loops | done: src/engine/menus.ts = fn 0032/0100/0382/0220/02e0/102b/09e0/0cd3/0c15/179b/17ff as tick-driven generators, plus int 9 (src/engine/keyboard.ts) and fn 2d5b (pollInput). game.html now starts at the title and reaches a race on real key presses; test/engine/menus.test.ts replays the capture's key script and stays pixel-exact through five screens. fn 1a4a (the player picks the three rivals after qualifying) included |
| Challenge mode | complete: the board between races (18d8/198e), the lives that the verdict gives and takes (1c1b tail), a driver knocked out every third win (1676 + 16de) and the champion screen (1aad) are ported as generators in src/engine/menus.ts and driven by game.html; src/engine/sequence.ts follows fn 13e4's tail and fn 10a0 for what comes after the results |
| Head to head | done (notes/51-head-to-head.md): one player (fn 0fbf) reuses the Challenge sequence with [03f8] = 1; two players get fn 1e20 (both character selects), 1ef1 (CHOOSE GAME!), 1faf (the eight-track tournament, first to four) and 2329 (SINGLE RACE over the ten fixed tracks), plus the HANDICAP prompt fn 0b51 and the results screen fn 256e/240a/2481/2216. The race side is fn 7759 (a car left behind), 78f8 (both back), the fn 7429 tail 75d2 (the 0x40-tick score animation), the fn 8dfc branch 8f03 (two-car ranking with the wrap correction), the 30df exit and the banners 851f/855a/8634 (BONUS / WINNER / PLAY OFF). fn 3f3b gives a two-player game its own AI table read, handicap included. Driven by game.html through a new 'race' request; test/engine/h2h.test.ts plays a whole match out to a winner |
